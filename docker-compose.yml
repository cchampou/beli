version: "3.3"
services:
  web:
    build: .
    image: beli:latest
    ports:
    - 9000:80
    volumes:
    - ./config/default.conf.template:/etc/nginx/templates/default.conf.template
    labels:
      - traefik.http.routers.web.rule=Host(`beli.cchampou.me`)
      - traefik.http.routers.web.tls=true
      - traefik.http.services.web.loadbalancer.server.port=9000
      - traefik.http.routers.web.tls.certresolver=main