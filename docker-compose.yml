version: "3.3"
services:
  web:
    build: .
    image: beli:latest
    ports:
    - 9000:80
    volumes:
    - ./config/default.conf.template:/etc/nginx/templates/default.conf.template
    labels:
      - traefik.http.routers.web-beli.rule=Host(`beli.cchampou.me`)
      - traefik.http.routers.web-beli.tls=true
      - traefik.http.services.web-beli.loadbalancer.server.port=9000
      - traefik.http.routers.web-beli.tls.certresolver=main