import styled from '@emotion/styled';
import { Items } from '../utils/flex';

const Card = styled(Items)`
  box-shadow: 0 5px 10px #DDDDDD;
  margin: 1rem;
  padding: 0;
`;

export const CardSubWrapper = styled('div')`
  padding: 0 1rem;
`;

export default Card;
